---
// ImageSlider.astro
---

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.css" />

<section style="padding:2rem;" data-aos="zoom-in">
  <div class="swiper-container">
    <div class="swiper">
      <div class="swiper-wrapper">
        <div class="swiper-slide slide-content">
          <h2>Exacto, siempre recordaré este día</h2>
          <img src="/antes1.jpg" alt="Antes" />
        </div>
        <div class="swiper-slide slide-content">
          <h2>No olvidaré tampoco la primera vez que te vi</h2>
          <img src="/despues2.jpg" alt="Después2" />
        </div>
        <div class="swiper-slide slide-content" style="text-align:center;">
          <h2>Y sobre todo, tengo en mente lo que nos espera</h2>
          <img src="/despues1.jpg" alt="Después" />
          <button
            onclick="window.location.href='/final'"
            class="slide-button animate__animated animate__bounceIn"
            style="margin:1.5rem auto; display:block;"
          >
            Pulsa para avanzar solete 🌸
          </button>
        </div>
      </div>
      <div class="swiper-pagination"></div>
      <div class="swiper-button-prev"></div>
      <div class="swiper-button-next"></div>
    </div>
  </div>
</section>


<script type="module">
  import Swiper from 'https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.mjs';

  const swiper = new Swiper('.swiper', {
    loop: false,
    pagination: { el: '.swiper-pagination' },
    navigation: {
      nextEl: '.swiper-button-next',
      prevEl: '.swiper-button-prev',
    },
  });

  const seguirBtn = document.querySelector('.slide-button');
  swiper.on('slideChange', () => {
    seguirBtn.style.display = swiper.activeIndex === 2 ? 'block' : 'none';
  });
</script>
